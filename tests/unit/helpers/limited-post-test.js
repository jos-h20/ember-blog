import { limitedPost } from 'stumptown-beans/helpers/limited-post';
import { module, test } from 'qunit';

module('Unit | Helper | limited post');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = limitedPost([42]);
  assert.ok(result);
});
